<local:VerdictView xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:Keysight.OpenTap.Wpf">
  <StackPanel Orientation="Horizontal">
    <Ellipse Name="indicator" Width="8" Height="8" VerticalAlignment="Center" Margin="0,1,2,0">
      <FrameworkElement.Style>
        <Style TargetType="{x:Type Ellipse}">
          <Setter Property="Shape.Fill" Value="{DynamicResource Control.Disabled.Foreground}"/>
          <Style.Triggers>
            <DataTrigger Binding="{Binding}" Value="Pass">
              <Setter Property="Shape.Fill" Value="{DynamicResource passColor}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding}" Value="Fail">
              <Setter Property="Shape.Fill" Value="{DynamicResource failColor}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding}" Value="Inconclusive">
              <Setter Property="Shape.Fill" Value="{DynamicResource warningColor}"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding}" Value="NotSet">
              <Setter Property="UIElement.Visibility" Value="Hidden"/>
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </FrameworkElement.Style>
    </Ellipse>
    <TextBlock Name="text" VerticalAlignment="Center" Text="{Binding}">
      <FrameworkElement.Style>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
          <Style.Triggers>
            <DataTrigger Binding="{Binding}" Value="NotSet">
              <Setter Property="UIElement.Visibility" Value="Hidden"/>
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </FrameworkElement.Style>
    </TextBlock>
  </StackPanel>
</local:VerdictView>